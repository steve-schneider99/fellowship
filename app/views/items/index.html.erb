<h2>This is the items home page</h2>

<ul>
  <li>List of items below</li>
  <% @items.each do |item| %>
    <li><%= link_to item.name, item_path(item) %></li>
  <% end %>
</ul>

<h2>Add a new item below:</h2>

<%= link_to "Click to add an item", new_item_path %>
